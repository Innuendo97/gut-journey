targets:
  $default:
    builders:
      drift_dev:
        options:
          # Store DateTime columns as ISO-8601 UTC text instead of unix
          # timestamps: exact instants, human-readable exports, no precision
          # loss.
          store_date_time_values_as_text: true
