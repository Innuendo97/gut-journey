/// The FODMAP groups a reintroduction path tests one at a time, in the
/// canonical order the guided path suggests them. Stored in the database by
/// name — add new values at the end, never rename.
enum FodmapGroup { fructans, gos, lactose, fructose, sorbitol, mannitol }
